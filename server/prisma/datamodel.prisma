type User {
    id: ID! @id
    email: String! @unique
    password: String!
    role: Role! @default(value: USER)
    createdAt: DateTime! @createdAt
    updatedAt: DateTime! @updatedAt
}

enum Role {
    USER
    ADMIN
}